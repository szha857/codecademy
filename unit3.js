
function unit3(){ 
var text = "Hello, it's Friday!";
var myName = "Wendy";
var hits =[];
for (i=0; i < text.length; i++){
 if (text[0] = text[i]){console.log("Your name wasn't found!")
     }
     
 else{     
     for(j=i; j < (text.length - i); j++){
         console.log(hits[j])
         }
     }
};

  };